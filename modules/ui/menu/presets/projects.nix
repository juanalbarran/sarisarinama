# modules/ui/menu/projects/projects.nix
# Placeholder so the root menu's reference resolves until the project menu
# exists in QML.
{
  flake.modules.homeManager.sarisarinama.programs.sarisarinama.menus.projects = [];
}
